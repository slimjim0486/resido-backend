{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "python -m scripts.seed_events",
    "cronSchedule": "0 2 * * *",
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
