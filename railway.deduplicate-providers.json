{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "python -m scripts.deduplicate --scope providers",
    "cronSchedule": "0 7 * * 1",
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
